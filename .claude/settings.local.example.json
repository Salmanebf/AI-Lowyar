{
  "_comment": "Copy this to settings.local.json and customize. settings.local.json is gitignored.",
  "permissions": {
    "allow": [
      "Bash(gh:*)",
      "Bash(open:*)"
    ]
  },
  "env": {
    "_comment": "Add personal env overrides here. NEVER put real secrets — those go in .env files."
  }
}
